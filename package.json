{
  "name": "admin-bridge",
  "version": "1.0.0",
  "description": "AdminBridge Application",
  "scripts": {
    "install-deps": "npm install && cd backend && npm install",
    "test-email": "node backend/test_email.js"
  },
  "dependencies": {
    "node-cron": "^3.0.3",
    "nodemailer": "^7.0.2"
  }
}
